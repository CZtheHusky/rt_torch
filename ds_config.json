{
    "train_batch_size" : 180,
    "train_micro_batch_size_per_gpu": 180,
    "gradient_accumulation_steps": 1,
    "optimizer": {
        "type": "Adam",
        "params": {
            "lr": 1e-4,
            "betas": [0.9, 0.999],
            "eps": 1e-8
        }
    },
    "scheduler": {
        "type": "CosineAnnealingLR",
        "params": {
            "eta_min": 1e-5,
            "T_max": 500000,
        }
    }
    "tensorboard": {
        "enabled": true,
        "job_name": "train",
        "output_path": /home/cz/bs/rt_torch/history/0321-173805-use-1e-4-1-1-0-256-deepspeed-fusion
    },

    "wall_clock_breakdown" : true,
    "checkpointing": {
        "enabled": true,
        "save_n_recent_checkpoints": 5,
        "max-ckpt-epochs": 5
    },
}
