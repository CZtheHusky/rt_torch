{
    "train_batch_size" : 1520,
    "train_micro_batch_size_per_gpu": 190,
    "gradient_accumulation_steps": 1,
    "steps_per_print": 1000,
    
    "fp16": {
        "enabled": true,
        "initial_scale_power": 12
    },

    "tensorboard": {
        "enabled": true,
        "job_name": "train",
    },

    "wall_clock_breakdown" : false,
    "checkpointing": {
        "enabled": true,
        "save_n_recent_checkpoints": 5,
        "max-ckpt-epochs": 5
    },
}